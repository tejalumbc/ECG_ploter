steps to run this project in your local machine

1 clone the repo using git URL of this project

2 navigate to this directory in local folder

3 install node modules by runing "npm  install" command in your terminal

4 to run this project "npm run dev" command in terminal

5 once the project is up and running follow instruction given on the page and upload a csv file to plot the ECG.
